{# Это макрос(функция) для вставки иконок svg, чтобы она работала ее нужно импортировать в файлы #}
{# На входе принимает имя файла иконки которое ищется в папке с иконками assets/icons #}
{% macro item(name) %}
    <span class="svg--icon {{name}}">
        <svg>
            <use xlink:href="#{{name}}"></use>
        </svg>
    </span>
{% endmacro %}